== dev todos
- switch all print, puts, (all external dependencies) into *one* library
- use a mock library
- figure out away to remove all of the ugly "this." while still having hooks for testing.
- switch to commonjs module system

== bugs / todo
- fix output so that it is synchronous when in verbose mode

- option parser:
    --verbose
    --port
    --version
    --help

-- commands:
  h / help
  h command
  l / list
    l
    l =
    l -
    l +
    l <num>
    list breakpoints
    list watchpoints
  c / continue
  b / break
    (with no args)
    filaname:num
    <filename>:num
  w / watchpoint (acts the same as a breakpoint - conditional breakpoint *on a line*)
    expr
    expr filename:num
    expr num
  d / delete
    d breakpoint
  n / next
  s / step
  r / repl
    start a node-debugger repl
  rw / raw_write
    rw <json>
  version
    display this version, v8 version, node version (process.version)?
  e / eval
    eval <expr>
    print
    set
  bt / backtrace

  sc / scope
  scs / scopes
  ct / context
    display context / callstack

- all events:
  - breakpoint
  - exception
  - everything else should generate an execption
  - break